<!--suppress ALL -->
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ContactsApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ContactsApp" parent="Base.Theme.ContactsApp" />

    <style name="CustomSearchView" parent="Widget.AppCompat.SearchView">
        <!-- SearchView text color -->
        <item name="queryTextColor">@android:color/black</item>
        <item name="queryHintTextColor">@android:color/darker_gray</item>

        <!-- SearchView background -->
        <item name="android:background">@drawable/search_view_background</item>

        <!-- SearchView icons -->
        <item name="searchIcon">@drawable/ic_search</item>
        <item name="closeIcon">@drawable/ic_close</item>
        <item name="goIcon">@drawable/ic_arrow_forward</item>
    </style>

</resources>